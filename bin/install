#!/usr/bin/env bash

source_path="${ASDF_DOWNLOAD_PATH}/mariadb-${ASDF_INSTALL_VERSION}-linux-systemd-x86_64.tar.gz"

tar xzf "${source_path}" -C "${ASDF_INSTALL_PATH}" --strip-components=1 || exit 1
